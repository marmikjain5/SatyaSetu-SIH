version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: satyadrishti-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: satyadrishti
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d satyadrishti"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
